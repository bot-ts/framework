import { Table } from "#database"

export interface {{ Name }} {
  // type of table
}

export default new Table<{{ Name }}>({
  name: "{{ name }}",
  description: "Table of {{ name }}",
  setup: (table) => {
    // setup table columns => http://knexjs.org/#Schema-Building
  },
})