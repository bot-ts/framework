import { Command } from "#core/command"

/**
 * See the {@link https://ghom.gitbook.io/bot.ts/usage/create-a-command guide} for more information.
 */
export default new Command({
  name: "{{ name }}",
  description: "The {{ name }} command",
  channelType: "all",
  async run(message) {
    // todo: code here
  }
})